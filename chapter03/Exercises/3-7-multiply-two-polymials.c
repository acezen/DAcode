// There is there algorithm to implement the multiply,each of
// them has the O(M^2N^2) , O(M^2N), O(MNlog(MN)) 

#include <stdio.h>
#include <stdlib.h>
#include "../polymial.h"

Polynomial MutiplyPolys1( Polynomial Poly1, Polynomial Poly2 )
{
    Polynimial product;
    PtrToNode Ptr1, Ptr2, Ptrp;

    product = MakeEmpty( NULL );
    // ...
}
